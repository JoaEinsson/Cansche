{
  "name": "cansche",
  "private": true,
  "version": "0.1.0",
  "description": "Fast calendar editor focused on batch editing and productivity",
  "packageManager": "pnpm@9.15.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "pnpm --filter @cansche/web dev",
    "build": "pnpm -r build",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vitest": "^1.4.0"
  }
}
